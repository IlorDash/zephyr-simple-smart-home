description: |
  Mock temperature sensor.

  Example definition in devicetree:

    mock_sensor: mock-sensor {
      compatible = "zephyr,mock-sensor";
      sample-freq = <1000>;
    };

include: sensor-device.yaml

compatible: "zephyr,mock-sensor"

properties:
  sample-freq:
    type: int
    description: Sample interval in mHz.